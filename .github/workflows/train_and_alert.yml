name: "🧠 Crypto AI Hybrid v13.8 - Fetch → Train → Spike → Alert"

on:
  schedule:
    - cron: "*/30 * * * *"   # Every 30 minutes
  workflow_dispatch:

permissions:
  contents: write
  actions: write   # ✅ needed for deleting old runs/artifacts

env:
  PYTHONUNBUFFERED: "1"

jobs:
  crypto-ai:
    runs-on: ubuntu-latest

    steps:
      # ---------------------------
      # 1️⃣ Setup and Checkout
      # ---------------------------
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          submodules: recursive

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      # ---------------------------
      # 🧰 Install dependencies (with cache)
      # ---------------------------
      - name: 💾 Cache Python dependencies
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: Install dependencies
        run: |
          echo "[SETUP] Installing Python dependencies..."
          python -m pip install --upgrade pip
          pip install -r requirements.txt || true
          pip install yfinance pandas numpy scikit-learn requests joblib matplotlib seaborn pycoingecko ccxt

      # ---------------------------
      # 2️⃣ Prepare required folders + Pre-Cleanup
      # ---------------------------
      - name: 🗂 Prepare required folders
        run: |
          mkdir -p models data_cache telemetry_logs utils training_summary models/spike_trend

      - name: 🧹 Pre-Cleanup Old Telemetry Logs
        run: |
          echo "[PRE-CLEANUP] Running telemetry_cleanup.py before fetching new data..."
          python telemetry_cleanup.py || echo "[WARN] Pre-cleanup encountered a non-critical issue."
          echo "[DONE] Old logs removed before new fetch and training."

      # ---------------------------
      # 3️⃣ Run Multi-Source Data Fetcher
      # ---------------------------
      - name: 📦 Run Multi-Source Data Fetcher
        run: |
          echo "[RUN] Fetching crypto data..."
          if [ -f "utils/data_fetcher.py" ]; then
            python utils/data_fetcher.py || { echo "[❌ ERROR] Data fetch failed."; exit 1; }
          elif [ -f "data_fetcher.py" ]; then
            python data_fetcher.py || { echo "[❌ ERROR] Root-level fetch failed."; exit 1; }
          else
            echo "[❌ ERROR] data_fetcher.py not found!"
            exit 1
          fi

      # ---------------------------
      # 4️⃣ Check model freshness
      # ---------------------------
      - name: ⏱ Check model freshness
        id: freshness
        run: |
          echo "[CHECK] Checking if models are older than 2 hours..."
          mkdir -p models
          needs_training=false

          for pair in BTCUSDT XRPUSDT GALAUSDT; do
            model="models/${pair}_model.pkl"
            if [ -f "$model" ]; then
              age_seconds=$(( $(date +%s) - $(stat -c %Y "$model") ))
              echo "[INFO] $model age: ${age_seconds}s"
              if [ $age_seconds -gt 7200 ]; then
                echo "[WARN] $model older than 2 hours — retraining required."
                needs_training=true
              fi
            else
              echo "[WARN] Missing model for $pair — retraining required."
              needs_training=true
            fi
          done

          echo "needs_training=$needs_training" >> $GITHUB_ENV

      # ---------------------------
      # 5️⃣ Train AI Models Conditionally
      # ---------------------------
      - name: 🧠 Train AI Models
        if: env.needs_training == 'true'
        run: python train_ai_model.py || echo "[WARN] Training script encountered an issue."

      - name: ✅ Skip Training (Models Fresh)
        if: env.needs_training == 'false'
        run: echo "[INFO] Models are fresh (<2h). Skipping training."

      # ---------------------------
      # 6️⃣ Run Spike Predictor
      # ---------------------------
      - name: ⚡ Run Spike Predictor
        run: |
          echo "[RUN] Executing spike predictor..."
          python spike_predictor.py || echo "[WARN] Spike predictor exited with non-zero status."
          echo "[INFO] Spike packs in models/spike_trend:"
          ls -lh models/spike_trend || echo "[WARN] No spike packs found."

      # ---------------------------
      # 7️⃣ Run Crypto AI Alert Logic (v11)
      # ---------------------------
      - name: 🚨 Run Crypto AI Alert Logic (v11)
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          EMAIL_HOST: ${{ secrets.EMAIL_HOST }}
          EMAIL_PORT: ${{ secrets.EMAIL_PORT }}
          EMAIL_USERNAME: ${{ secrets.EMAIL_USERNAME }}
          EMAIL_PASSWORD: ${{ secrets.EMAIL_PASSWORD }}
        run: |
          python crypto_ai_alert_v10.py || echo "[WARN] Alert logic exited with non-zero status."

      # ---------------------------
      # 8️⃣ Upload Trained Models
      # ---------------------------
      - name: 💾 Upload Trained Models
        uses: actions/upload-artifact@v4
        with:
          name: trained_models
          path: models/
          retention-days: 7

      # ---------------------------
      # 9️⃣ Post-Cleanup + Upload Telemetry Logs
      # ---------------------------
      - name: 🧹 Post-Cleanup Telemetry Logs
        run: |
          echo "[POST-CLEANUP] Running telemetry_cleanup.py after full processing..."
          python telemetry_cleanup.py || echo "[WARN] Post-cleanup encountered a non-critical issue."
          echo "[DONE] Logs trimmed and folder cleaned after run."

      - name: 📊 Upload Telemetry Logs
        uses: actions/upload-artifact@v4
        with:
          name: telemetry_logs
          path: telemetry_logs/
          retention-days: 7

      # ---------------------------
      # 🔟 🧹 Remove Old Workflow Runs (keep last 2)
      # ---------------------------
      - name: 🧩 Cleanup Cache & Old Workflow Artifacts
        if: github.ref == 'refs/heads/main'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          echo "[CLEANUP] Removing old workflow artifacts (keep last 2)..."
          gh run list --limit 20 --json databaseId,status --jq '.[] | select(.status=="completed") | .databaseId' | tail -n +3 | while read run_id; do
            echo "[DELETE] Removing old workflow run $run_id"
            gh run delete $run_id || echo "[⚠️ Skip] Missing permission or already removed."
          done
          echo "[DONE] Kept last 2 workflow artifacts only."

      # ---------------------------
      # 11️⃣ Commit and Push Models + Telemetry
      # ---------------------------
      - name: 🪶 Commit and Push Models + Telemetry
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config user.email "actions@github.com"
          git config user.name "github-actions[bot]"
          git add -A models telemetry_logs data_cache training_summary || true
          if git diff --cached --quiet; then
            echo "[INFO] No changes detected, skipping commit."
          else
            echo "[COMMIT] Committing updated models + telemetry after cleanup..."
            git commit -m "🤖 Auto-update: Models + Telemetry Cleanup ($(date '+%Y-%m-%d %H:%M:%S')) [skip ci]"
            git pull --rebase origin main || echo "[WARN] Rebase skipped or failed."
            git push origin main || echo "[WARN] Push failed (check permissions)."
          fi

      # ---------------------------
      # ✅ Finish
      # ---------------------------
      - name: ✅ Done
        run: echo "[FINISH] Crypto AI Hybrid v13.8 workflow completed successfully."
