name: "🧠 Crypto AI Hybrid v13.5 - Train & Alert"

on:
  schedule:
    - cron: "*/30 * * * *"   # Every 30 minutes
  workflow_dispatch:

permissions:
  contents: write
  actions: read

env:
  PYTHONUNBUFFERED: "1"

jobs:
  crypto-ai:
    runs-on: ubuntu-latest

    steps:
      # ---------------------------
      # 1️⃣ Setup and Checkout
      # ---------------------------
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          echo "[SETUP] Installing Python dependencies..."
          python -m pip install --upgrade pip
          pip install -r requirements.txt || true
          pip install yfinance pandas numpy scikit-learn requests joblib matplotlib seaborn pycoingecko ccxt

      # ---------------------------
      # 2️⃣ Prepare required folders
      # ---------------------------
      - name: 🗂 Prepare required folders
        run: |
          echo "[INIT] Ensuring models, data_cache, telemetry_logs exist..."
          mkdir -p models data_cache telemetry_logs utils training_summary

      # ---------------------------
      # 3️⃣ Run unified data fetcher
      # ---------------------------
      - name: 📦 Run Multi-Source Data Fetcher
        run: |
          echo "[RUN] Fetching crypto data using utils/data_fetcher.py..."
          if [ -f utils/data_fetcher.py ]; then
            python utils/data_fetcher.py || echo "[WARN] Data fetch fallback triggered — continuing with cache."
          else
            echo "[ERROR] utils/data_fetcher.py not found — please verify repository structure."
          fi

      # ---------------------------
      # 4️⃣ Check model freshness
      # ---------------------------
      - name: ⏱ Check model freshness
        id: freshness
        run: |
          echo "[CHECK] Checking if models are older than 2 hours..."
          needs_training=false
          for model in models/*_model.pkl; do
            if [ -f "$model" ]; then
              age_seconds=$(( $(date +%s) - $(stat -c %Y "$model") ))
              echo "[INFO] $model age: ${age_seconds} seconds"
              if [ $age_seconds -gt 7200 ]; then
                echo "[WARN] $model older than 2 hours — retraining required."
                needs_training=true
              fi
            else
              echo "[WARN] Missing model file ($model) — retraining required."
              needs_training=true
            fi
          done
          echo "needs_training=$needs_training" >> $GITHUB_ENV

      # ---------------------------
      # 5️⃣ Train AI Models Conditionally
      # ---------------------------
      - name: 🧠 Train AI Models
        if: env.needs_training == 'true'
        run: |
          echo "[TRAIN] Starting model training..."
          python train_ai_model.py || echo "[WARN] Training script encountered an issue."

      - name: ✅ Skip Training (Models Fresh)
        if: env.needs_training == 'false'
        run: echo "[INFO] Models are up-to-date (younger than 2 hours). Skipping training."

      # ---------------------------
      # 6️⃣ Run Spike Predictor (Updated filename)
      # ---------------------------
      - name: ⚡ Run Spike Predictor
        run: |
          echo "[RUN] Executing spike predictor..."
          if [ -f spike_predictor.py ]; then
            python spike_predictor.py || echo "[WARN] Spike predictor exited with non-zero status."
          else
            echo "[INFO] spike_predictor.py not found, skipping."
          fi

      # ---------------------------
      # 7️⃣ Run Crypto AI Alert Logic (v10)
      # ---------------------------
      - name: 🚨 Run Crypto AI Alert Logic (v10)
        run: |
          echo "[RUN] Executing crypto alert logic..."
          if [ -f crypto_ai_alert_v10.py ]; then
            python crypto_ai_alert_v10.py || echo "[WARN] Alert logic exited with non-zero status."
          else
            echo "[ERROR] crypto_ai_alert_v10.py not found — please verify file path."
          fi

      # ---------------------------
      # 8️⃣ Upload Trained Models
      # ---------------------------
      - name: 💾 Upload Trained Models
        uses: actions/upload-artifact@v4
        with:
          name: trained_models
          path: models/

      # ---------------------------
      # 9️⃣ Upload Telemetry Logs
      # ---------------------------
      - name: 📊 Upload Telemetry Logs
        uses: actions/upload-artifact@v4
        with:
          name: telemetry_logs
          path: telemetry_logs/

      # ---------------------------
      # 🔟 Commit and Push Models + Telemetry
      # ---------------------------
      - name: 🪶 Commit and Push Models + Telemetry
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          echo "[GIT] Saving models and telemetry logs..."
          git config user.email "actions@github.com"
          git config user.name "github-actions[bot]"
          git add -f models/*.pkl telemetry_logs/*.json data_cache/* training_summary/* || true
          if git diff --cached --quiet; then
            echo "[INFO] No changes detected, skipping commit."
          else
            git commit -m "🤖 Auto-update: Models + Telemetry ($(date '+%Y-%m-%d %H:%M:%S'))"
            git pull --rebase origin main || echo "[WARN] Rebase skipped or failed."
            git push origin main || echo "[WARN] Push failed (check permissions)."
          fi

      # ---------------------------
      # ✅ Finish
      # ---------------------------
      - name: ✅ Done
        run: echo "[FINISH] Crypto AI Hybrid v13.5 workflow completed successfully."
